<%= will_paginate @places %>

<% @places.each do |place| %>
    <div class="booyah-box col">
    <h1><%= place.name%></h1>
    <i><%= place.address %></i><br /><br />
    <p><%= place.description %></p><br />
    </div>
    
<% end %>